# 文档生态守护报告 2026-06-11

## 范围

- 模式：全量守护。
- 代码探索：后端、前端、Docker Compose、README、AGENTS、CLAUDE、architecture、development、roadmap。
- Git 锚定：当前目录不是 Git 仓库，无法找到最近文档 commit；本次按首次运行做全量探索。

## 代码事实摘要

- 后端是 FastAPI + async SQLAlchemy + SQLite，入口在 `backend/app/main.py`。
- 数据库文件和上传图片都在 `data/`，Compose 把宿主机 `./data` 挂到容器 `/code/data`。
- 任务主模型是 `important + due_date`；旧 `urgency / importance` 只保留兼容老库。
- 每日面板由 `created_date <= D 且 (completed_date 为空 或 >= D)` 查询得到，没有每日快照表。
- 状态为 `todo / doing / review / done`，只有 `done` 写入 `completed_date`。
- 前端是 React 19 + Vite 6 + TypeScript，无 UI 库，单屏应用，没有路由库和状态库。
- Vite 代理 `/api` 和 `/uploads` 到后端容器 `backend:8000`。
- AI 拆任务走 OpenAI 兼容 `/chat/completions`，只返回草稿，不直接入库。

## 四种操作统计

- 更新：8 处。
- 新增：1 处。
- 删除：0 处。
- 审计问题：0 个代码问题；1 个环境限制（无 Git 仓库，无法自动提交）。

## 已执行修改

- `AGENTS.md`：修正旧 `urgency / importance` 象限说明，补充拖拽字段和排序事实，AI 说明改为 OpenAI 兼容接口。
- `CLAUDE.md`：把依赖描述从伪精确版本号改为与依赖文件一致的范围表述。
- `README.md`：修正产品定位、目录结构和未来能力描述。
- `docs/architecture/README.md`：补齐真实模块职责、数据流、排序和图片复制设计。
- `docs/architecture/frontend.md`：补齐状态流、拖拽规则、完成折叠、弹窗和图片生命周期。
- `docs/architecture/backend.md`：补齐 session 链路、轻量迁移、图片删除策略、AI 容错和错误处理。
- `docs/development/getting-started.md`：补齐环境要求、日志/停止命令、依赖变更提示和项目结构。
- `docs/development/deployment.md`：补齐环境变量表、依赖重建、恢复步骤、回滚和本机检查清单。
- `docs/development/changelog.md`：记录本次文档同步。
- `docs/roadmap/`：把已完成的文档事实同步项标为完成。

## Spec / ADR / Gate 审计

- `docs/specs/`：目录不存在，无可审计 spec。
- `[command: ...]` Gate：未发现。
- `docs/architecture/adr/`：只有 `README.md` 索引，暂无正式 ADR，无有效性冲突。

## 未修改的代码问题

本次未发现必须通过 `/implement` 修复的代码-文档冲突。需要注意的是当前目录没有 Git 仓库，文档工作流中的 commit 步骤无法执行。

## 建议后续

- 如要严格走提交记录，先在项目目录初始化或恢复 Git 仓库，再提交本次文档改动。
- 后续新增字段时，继续沿用 `database.py` 的 `PRAGMA table_info` + `ALTER TABLE` 轻量迁移模式。
- Phase 4 可优先补最小后端接口测试和前端 `npm run build` 门禁说明。

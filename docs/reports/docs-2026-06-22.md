# 文档生态守护报告 — 2026-06-22

> 范围：全量守护（architecture + development + CLAUDE.md）。
> 变更锚定：自上次架构文档更新 `dd3d990`（docs: 补充数据库轻量迁移策略）以来的全部源码变更。

## 一、变更锚定

自 `dd3d990` 以来新增 / 大改的源文件，按模块归组：

| 模块 | 变更文件 | 文档此前是否覆盖 |
|------|---------|----------------|
| 后端维护 | `routers/maintenance.py`（新增）、`orphan_uploads.py`（新增）、`scripts/cleanup_orphan_uploads.py` | ❌ 完全缺失 |
| 后端 AI | `routers/ai.py`（去重上下文、超时、批量解析） | ⚠️ 部分过时 |
| 前端视图 | `components/TaskScatter.tsx`（新增）、`components/ErrorBoundary.tsx`（新增） | ❌ 完全缺失 |
| 前端逻辑 | `taskViews.ts`、`taskReview.ts`、`taskReports.ts`、`statusMeta.ts` | ❌ 大部分缺失 |
| 前端容器 | `App.tsx`（视图/布局切换、搜索筛选、周回顾、备份、AI prefill） | ⚠️ 状态清单过时 |
| 前端 AI | `components/AiQuickAdd.tsx`（多行、模板、上一条、批量微调、35s 超时） | ⚠️ 过时 |

## 二、四种操作明细

### 🆕 新增（代码有、文档缺）

1. backend.md：维护接口三端点 + `orphan_uploads.py` 对账语义。
2. backend.md：AI 去重上下文 `existing_titles`、30s 超时、`MAX_DRAFTS=12`、`temperature=0.3`。
3. frontend.md：`TaskScatter` 散点布局与坐标映射。
4. frontend.md：`ErrorBoundary` 渲染兜底。
5. frontend.md：视图筛选 / 收口建议（`taskViews.ts`）。
6. frontend.md：周回顾统计（`taskReview.ts`）。
7. frontend.md：导出与同步（`taskReports.ts`）。
8. frontend.md：备份弹窗接 `/api/maintenance/summary`。
9. frontend.md：AI 草稿队列回退 / 批量微调。
10. frontend.md：全局键盘导航（`[ ] T N F 1/2/3 4 / Esc`）。
11. README.md：模块清单补 maintenance / orphan_uploads / TaskScatter / ErrorBoundary / 前端工具模块。
12. README.md：散点、维护对账、AI 去重三条非直觉决策。
13. getting-started.md：项目结构补后端维护模块、前端工具模块、`scripts/`。
14. CLAUDE.md：项目结构补 maintenance 路由与散点 / 错误边界组件。

### ⚠️ 更新（文档对不上代码）

15. frontend.md：`App.tsx` 状态清单（补布局、筛选、周回顾、备份、AI prefill 等）。
16. frontend.md：组件分层补 `TaskScatter` / `ErrorBoundary`，`AiQuickAdd` 与 `TaskCard` 描述对齐。
17. frontend.md：拖拽启用条件改为「当前视图且无筛选」。
18. frontend.md：键盘交互段重写并标注走 `useDocumentEvent`。
19. frontend.md：手动回归重点补搜索筛选、散点、周回顾、导出、备份。
20. backend.md：请求链路补 maintenance router。
21. README.md：`App.tsx` 与 `TaskCard` 职责描述对齐。

### ❌ 删除（文档引用已删除的代码）

- 无。复核现有文档无指向已删除文件 / 函数的悬空引用；backend.md「无 Service/Repository 分层、无认证」仍准确。

### 🔍 审计（spec-code / ADR / Gate）

- `docs/specs/` 目录不存在 → spec-code 一致性审计 N/A。
- `docs/architecture/adr/` 仅有索引、无正式 ADR → ADR 有效性审计 N/A。
- 未发现 `[command:]` 形式的 Gate → Gate 可执行性审计 N/A。

## 三、历史趋势

- 上次审计 `docs-2026-06-11.md`：以视图状态与日期语义同步为主。
- 本次：21 处架构文档同步，主因是 Phase 5 / Phase 6 的维护接口、散点视图、筛选回顾、AI 增强累积未回写文档。问题集中在「新增模块未建档」，非「文档与代码相互矛盾」，属可控的滞后型欠债。

## 四、需用户关注（代码问题，本次未改）

- 无代码不一致项。本次所有差异都是文档滞后于已正确实现的代码，未发现代码偏离约定（旧 `urgency/importance` 字段仅在 `database.py` 迁移逻辑中读取，符合「兼容老库」约定）。
- 提醒：`docs/roadmap/phase-6` 标记进行中 10/11，本次未改 roadmap 勾选；功能落地后的 checkbox 同步建议随 `/implement` 或 `/release` 流程处理。

## 五、下一步

- `git push`（如需推送 61 个未推送 commit + 本次文档提交）。
- 无需 `/implement` 修复代码。

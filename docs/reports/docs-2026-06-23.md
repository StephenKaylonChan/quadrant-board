# 文档生态守护报告 — 2026-06-23

> 范围：全量。锚定基准 `ed08284 feat(auth)`（代码与首批文档同提交，无未文档化的源码变更）。
> 触发：登录鉴权功能开发 + 生产上线后的文档全量同步。

## 四种操作统计

- 更新：2 处（backend.md「没有鉴权」表述、请求链路）
- 新增：8 处（backend.md 登录鉴权节、frontend.md auth 状态与组件、getting-started 鉴权节与结构、changelog 条目、CLAUDE/AGENTS 结构与决策、DEPLOYMENT 上线行）
- 删除：0 处（无过时文档；`docs/reports/` 历史报告按规约保留）
- 审计问题：0 处（无 spec / ADR；Gate 命令均有效）

## 文档改动清单

- `docs/DEPLOYMENT.md`：補「鉴权 2026-06-23 在生产启用并灌入本地真实数据」一行。
- `docs/architecture/backend.md`：请求链路加 auth 路由 + `require_auth` 守卫；替换「没有认证鉴权」过时表述；新增「登录鉴权」一节（凭据存储 / 开关种子 / 登录态 / 守卫 / 接口）；`init_db` 补 `_seed_credential` 说明。
- `docs/architecture/frontend.md`：App.tsx 状态清单加 `authReady/requireLogin/authEnabled/username/accountOpen`；组件分层加 `LoginGate`/`AccountModal`；`api.ts` 补 cookie 与 401 统一处理。
- `docs/development/getting-started.md`：新增「登录鉴权（本地默认关闭）」节；项目结构补 `auth.py`/`routers/auth.py`/登录页/账号弹窗。
- `docs/development/changelog.md`：[Unreleased] Added 加登录鉴权与生产上线两条。
- `CLAUDE.md`：项目结构补 `routers/auth.py`/`auth.py`；关键架构决策加鉴权开关与凭据存储一条。
- `AGENTS.md`：核心设计加登录鉴权一条（新增受保护路由记得挂守卫）。

## 记忆文档审视

- 4 个记忆文件（visual-iteration-playwright-mcp / anchor-real-environment-and-pain / prefer-robust-over-patch / prefer-complete-user-facing-features）均现行有效，无过时项。
- `MEMORY.md` 索引与文件一致，无孤儿条目。
- 服务器连接信息按用户意愿**未落盘**（仅本次会话使用）。

## 需用户关注（代码问题，本次未改）

- 无代码不一致。遗留硬化项（`/uploads` 守卫、登录限流）属 backlog，非文档问题。
